const DeleteIcon = ({ size, color }) => {
    const iconStyle = {
      fontSize: size,
      color: color,
     

    };

    
  
    return (
      <i className="fa-solid fa-trash" style={iconStyle}></i>
    );
  };
  
  
  
  export default DeleteIcon;
  
