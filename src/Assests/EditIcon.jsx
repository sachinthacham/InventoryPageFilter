

const EditIcon = ({size}) => {
    const iconStyle = {
        fontSize:size
    }
    return(
        <i className="fas fa-edit" style={iconStyle}></i>
    );
    
    
  };
  
  export default EditIcon;
